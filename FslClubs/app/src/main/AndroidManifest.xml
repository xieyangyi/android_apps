<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fsl.fslclubs" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity android:name=".main.MainActivity" android:label="FSL">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="android.app.default_searchable"
                android:value="com.fsl.fslclubs.main.SearchResultActivity" />
        </activity>

        <activity android:name=".login.LoginActivity" android:label="@string/login" ></activity>
        <activity android:name=".login.RegisterActivity" android:label="@string/register"></activity>

        <activity android:name=".events.EventsActivity" android:label="@string/activities"></activity>

        <activity android:name=".myEvents.MyEventActivity" android:label="@string/my_activity"></activity>
        <activity android:name=".myEvents.AlarmActivity" android:label="@string/alarm_settings"></activity>

        <activity android:name=".clubs.ClubsActivity" android:label="@string/clubs"></activity>

        <activity android:name=".me.AboutActivity" android:label="@string/head_about"></activity>
        <activity android:name=".me.SettingsActivity" android:label="@string/head_settings"></activity>
        <activity android:name=".me.PersonInfoActivity" android:label="@string/head_person_info"></activity>

        <activity android:name=".main.SearchResultActivity" android:label="@string/search_result">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data android:name="android.app.searchable" android:resource="@xml/searchables"/>
        </activity>

        <provider android:name=".login.UserInfoProvider"
            android:authorities="com.fsl.fslclubs.provider" />
        <provider android:name=".myEvents.MyEventProvider"
            android:authorities="com.fsl.fslclubs.myactivity.provider" />

        <receiver android:name=".myEvents.AlarmReceiver">
            <intent-filter>
                <action android:name="com.fsl.fslclubs.AlarmReceiver" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
</manifest>
